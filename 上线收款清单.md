# PolishMail 上线收款清单

## 1. 先在 Stripe 创建这些价格
- Pro 月卡
- 周卡
- 日语邮件包
- 英语邮件包
- 中文职场包

## 2. 把这些值填进 `.env`
- `STRIPE_SECRET_KEY`
- `STRIPE_WEBHOOK_SECRET`
- `STRIPE_PRICE_PRO_MONTH`
- `STRIPE_PRICE_PRO_WEEK`
- `STRIPE_PRICE_JP_PACK`
- `STRIPE_PRICE_EN_PACK`
- `STRIPE_PRICE_CN_PACK`
- `APP_BASE_URL`
- `APP_COOKIE_SECRET`

## 3. 本地验证
启动：

```bash
npm install
cp .env.example .env
npm run dev
```

访问：

`http://localhost:8787`

## 4. 真正上线
可以部署到：
- Railway
- Render
- Fly.io
- 自己的 VPS

如果你用 Docker，直接用项目里的 `Dockerfile`

## 5. 上线后先别做太多功能
先测试这三件事：
- 有没有人真的点击付款
- 哪个套餐最容易成交
- 日语场景的使用率是不是最高

## 5.1 建议再配置 Stripe webhook
地址：

`https://你的域名/api/stripe-webhook`

事件：

- `checkout.session.completed`

这样支付链路会更稳。

## 6. 最值得先推广的标题
- 日本留学生给教授发邮件，不想每次都用 GPT 反复改
- 就活邮件不会写？10 秒生成可直接发送的日语邮件
- 给 HR / 教授 / 上司写正式邮件，直接出 3 个版本
